<!DOCTYPE html>
<html>
  <!-- head et metas -->
  <head>
    <!-- Start cookieyes banner -->
    <script id="cookieyes" type="text/javascript" src="https://cdn-cookieyes.com/client_data/58739e9c6f147a3d8f087883/script.js"></script>
    <!-- End cookieyes banner -->
    <title>My Ikigaï : studio web en Tarentaise </title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="My Ikigaï est un studio de développement web, situé à Bourg-Saint-Maurice en Tarentaise en Savoie en France. Il est spécialisé dans le développement fullstack d'applications mobile et de sites web. Une démarche mobile First et orienté utilisateur. Nous proposons des packs pour les créateurs d'entreprise.">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="index, follow">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="<%= asset_path 'logo.png' %>" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200;12..96,300;12..96,400;12..96,500;12..96,600;12..96,700;12..96,800&family=Bubbler+One&family=Italiana&family=Poiret+One&family=Poppins&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Advent+Pro:wght@800;900&family=Londrina+Shadow&family=Londrina+Solid:wght@100;300&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>

    <script>document.addEventListener('DOMContentLoaded', function() {
      var logo = document.querySelector('.navbar-brand img'); // Sélectionne l'image du logo
      var hauteurDisparition = 100; // Définissez la hauteur de défilement à laquelle le logo doit disparaître

      function verifierPositionScroll() {
        if (window.scrollY > hauteurDisparition) {
          logo.style.opacity = '0'; // Rend le logo invisible
          logo.style.transition = 'opacity 0.5s'; // Ajoute une transition pour lisser la disparition
        } else {
          logo.style.opacity = '1'; // Rend le logo à nouveau visible
        }
      }
      // Écouter pour le défilement de la fenêtre
      window.addEventListener('scroll', verifierPositionScroll);
    });</script>

    <%= render 'shared/banner' %>
      <p class="alert"><%= alert %></p>
      <p class="notice"><%= notice %></p>
    <%= render 'shared/flashes' %>
    <div class="main-container">
    <%= yield %>
    </div>
    <footer>
    <%= render 'shared/footer' %>

    </footer>
  </body>
</html>
